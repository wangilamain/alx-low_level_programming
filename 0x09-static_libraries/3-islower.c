#include<stdio.h>
#include"main.h"
/**
 * _islower- _islower entry*
 * description: Function prototypes*
 * @c: the check of lower case*
 * Return: Always 0 (success)
 */
int _islower(int c)
{return (c >= 'a' && c <= 'z'); }
